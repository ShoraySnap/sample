<UserControl
    x:Class="SnaptrudeManagerAddin.Views.ModelExportedView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:SnaptrudeManagerAddin.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:Background="White"
    d:DesignHeight="272"
    d:DesignWidth="560"
    FontSize="13"
    mc:Ignorable="d">
    <UserControl.Resources>
        <ResourceDictionary Source="pack://application:,,,/SnaptrudeManagerAddin;component/UI/Resources/ResourceDictionary.xaml" />
    </UserControl.Resources>
    <StackPanel VerticalAlignment="Center">
        <TextBlock
            Width="300"
            FontFamily="{StaticResource Inter500}"
            FontWeight="Medium"
            Foreground="{StaticResource Colors/Neutral/800}"
            Text="The model was exported successfully!"
            TextAlignment="Center"
            TextWrapping="Wrap" />
        <Button
            Margin="20"
            HorizontalAlignment="Center"
            Command="{Binding LaunchCommand}"
            Content="Launch Snaptrude"
            Style="{DynamicResource Button_Primary}" />
    </StackPanel>
</UserControl>
